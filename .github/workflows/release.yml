name: Release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  build-packages:
    name: Build packages
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4.2.2

      - name: Install build tools
        run: sudo apt-get install -y dpkg-dev rpm rpm-sign

      - name: Set version from tag
        run: |
          TAG="${GITHUB_REF_NAME#v}"          # strip leading v  (e.g. 1.18.0)
          RPM_VER="${TAG//-/.}"               # RPM forbids '-'; replace with '.'
          echo "VERSION=$TAG" >> "$GITHUB_ENV"
          # Patch version into the spec
          awk -v v="Version:        $RPM_VER" '/^Version:/{print v; next} 1' \
            packaging/koet.spec > /tmp/_spec && mv /tmp/_spec packaging/koet.spec
          # Patch debian control
          awk -v v="Version: $TAG" '/^Version:/{print v; next} 1' \
            packaging/debian/control > /tmp/_ctrl && mv /tmp/_ctrl packaging/debian/control
          # Patch debian changelog
          awk -v v="koet ($TAG-1) unstable; urgency=low" \
            'NR==1{print v; next} 1' \
            packaging/debian/changelog > /tmp/_cl && mv /tmp/_cl packaging/debian/changelog
          # Patch build-pkg.sh VERSION variable
          awk -v v="VERSION=\"$RPM_VER\"" '/^VERSION=/{print v; next} 1' \
            packaging/build-pkg.sh > /tmp/_bpkg && chmod +x /tmp/_bpkg && mv /tmp/_bpkg packaging/build-pkg.sh

      - name: Build RPM and DEB
        run: ./packaging/build-pkg.sh

      - name: Import GPG signing key
        run: |
          echo "${{ secrets.GPG_PRIVATE_KEY }}" | gpg --batch --import
          KEY_ID=$(gpg --list-secret-keys --keyid-format LONG 2>/dev/null \
            | grep "^sec" | head -1 | awk '{print $2}' | cut -d'/' -f2)
          echo "GPG_KEY_ID=$KEY_ID" >> "$GITHUB_ENV"
          mkdir -p ~/.gnupg
          echo "allow-loopback-pinentry" >> ~/.gnupg/gpg-agent.conf
          gpgconf --kill gpg-agent
          printf '%%_gpg_name %s\n%%__gpg /usr/bin/gpg\n%%__gpg_sign_cmd %%{__gpg} gpg --batch --no-verbose --no-armor --no-secmem-warning -u "%%{_gpg_name}" -sbo %%{__signature_filename} --digest-algo sha256 %%{__plaintext_filename}\n' "$KEY_ID" > ~/.rpmmacros

      - name: Sign RPM
        run: |
          for rpm in dist/*.rpm; do
            rpmsign --addsign "$rpm"
            echo "Signed: $rpm"
          done

      - name: Verify RPM signature
        run: |
          rpm --import packaging/RPM-GPG-KEY-koet
          for rpm in dist/*.rpm; do
            rpm --checksig "$rpm" && echo "OK: $rpm signature valid"
          done

      - name: List artifacts
        run: ls -lh dist/

      - name: Build release body
        run: python3 .github/scripts/build_release_body.py
        env:
          GITHUB_REPOSITORY: ${{ github.repository }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2.3.2
        with:
          name: "KOET ${{ env.VERSION }}"
          body_path: /tmp/release_body.md
          files: |
            dist/*.rpm
            dist/*.deb
            packaging/RPM-GPG-KEY-koet
          draft: false
          prerelease: ${{ contains(github.ref_name, '-') }}

      - name: Delete old releases (keep newest 3)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release list --repo "$GITHUB_REPOSITORY" --limit 100 --json tagName,createdAt \
            --jq 'sort_by(.createdAt) | reverse | .[3:] | .[].tagName' \
          | while read -r tag; do
              echo "Deleting release $tag"
              gh release delete "$tag" --repo "$GITHUB_REPOSITORY" --yes --cleanup-tag
            done
